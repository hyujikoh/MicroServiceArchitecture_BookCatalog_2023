apply plugin: "war"

bootWar {
    mainClassName = "com.my.bookcatalog.BookCatalogApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
